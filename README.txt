The videos of our filter running on dataset 1 and 4 are in the /videos directory

To run the code:

	mkdir build && cd build
	cmake .. && make && ../bin/ParticleFilter


Some dependencies you'll need are:

	sudo apt-get install cmake libopencv-dev build-essential

